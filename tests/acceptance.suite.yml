actor: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
        - \Helper\Site
    config:
        WebDriver:
            url: http://tests.chrome
            host: 'tests.chrome'
            browser: 'chrome'
            window_size: 1920x1280
            restart: true
groups: